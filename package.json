{
  "name": "@kiket/cli",
  "version": "0.1.0",
  "private": true,
  "description": "CI-friendly Kiket CLI for config, scans, evidence, reports, and audit proof verification",
  "type": "module",
  "bin": {
    "kiket": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "lint": "biome check --diagnostic-level=error src/client.ts src/commands.ts src/index.ts src/local-config.ts tests/commands.test.ts",
    "test": "vitest run tests/commands.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kiket/api-client": "workspace:*",
    "@kiket/engine": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
